# 代码质量门禁——CI 阻塞关卡（ci.yml 无 continue-on-error）。
# 规则集：F(硬错误) + BLE(盲捕异常) + DTZ(显式时区,统一UTC) + B(bugbear)。
# B008 忽略：FastAPI 的 Depends(...) 依赖注入惯用法属误报。
# 注：ruff 0.16 要求 line-length 为顶级键（不能放在 [lint] 内）。
line-length = 120

[lint]
select = ["F", "BLE", "DTZ", "B"]
ignore = ["B008"]
