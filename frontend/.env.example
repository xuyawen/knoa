# 演示账号自动填充（生产构建生效，本地开发不启用）
# 本文件是生产构建的账号来源：Dockerfile 在构建时会把它复制为 .env（Vite 只认 .env）。
# 本地 npm run dev 永远不填充（demo.ts 里已用 import.meta.env.PROD 网关住）。
# 改账号直接改这里并提交即可；VITE_DEMO_FILL=false 可彻底关闭自动填充。
VITE_DEMO_FILL=true
VITE_DEMO_ACCOUNT=CP000002
VITE_DEMO_PASSWORD=q123321.
