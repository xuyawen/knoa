# 依赖 / 缓存（注意：uv.lock 与 pyproject.toml 必须进镜像供 uv sync 使用，不能忽略）
.venv/
__pycache__/
*.pyc
*.pyo

# 密钥 / 证书 / 环境（绝不进镜像）
.env
.env.example
certs/

# 日志 / 测试 / 临时
*.log
backend.log
tests/
test_agnes.py

# git / 编辑器
.git/
.vscode/
.idea/

# 前端产物（后端镜像不需要）
node_modules/
dist/
